insert INTO Record (
    game_id,
    user_id,
    recordMinutes
) values (1,1,1776);
insert INTO Record (
    game_id,
    user_id,
    recordMinutes
) values (1,2,54);
insert INTO Record (
    game_id,
    user_id,
    recordMinutes
) values (1,3,1086);
insert INTO Record (
    game_id,
    user_id,
    recordMinutes
) values (1,4,2874);
insert INTO Record (
    game_id,
    user_id,
    recordMinutes
) values (1,5,384);
insert INTO Record (
    game_id,
    user_id,
    recordMinutes
)values (1,6,66);

insert INTO Record (
    game_id,
    user_id,
    recordMinutes
)values (1,7,24);

insert INTO Record (
    game_id,
    user_id,
    recordMinutes
)values (1,8,6);

insert INTO Record (
    game_id,
    user_id,
    recordMinutes
)values (1,9,36);

insert INTO Record (
    game_id,
    user_id,
    recordMinutes
)values (1,10,999);
