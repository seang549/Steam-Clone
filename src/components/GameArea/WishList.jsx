const WishList = () => {
  return (
    <div id='wishList'>
      <p>
        <a href='#'>Sign in</a> to add this item to your wishlist, follow it, or
        mark it as ignored
      </p>
    </div>
  );
};

export default WishList;
